let dogarray = [];

const dogs = ["adog","bdog","cdog"];

dogs.forEach((doggiename, indexx) => {
  if (doggiename === "adog") {
    console.log('adog');
  } else {
    console.log(doggiename, indexx);
  }

});
